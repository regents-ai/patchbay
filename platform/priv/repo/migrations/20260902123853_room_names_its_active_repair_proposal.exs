defmodule Patchbay.Repo.Migrations.RoomNamesItsActiveRepairProposal do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create unique_index(:repair_proposals, [:id, :room_id],
             name: "repair_proposals_unique_id_per_room_index"
           )

    alter table(:rooms) do
      modify :active_repair_proposal_id,
             references(:repair_proposals,
               column: :id,
               with: [id: :room_id],
               name: "rooms_active_repair_proposal_id_fkey",
               type: :uuid,
               prefix: "public"
             )
    end
  end

  def down do
    drop constraint(:rooms, "rooms_active_repair_proposal_id_fkey")

    alter table(:rooms) do
      modify :active_repair_proposal_id, :uuid
    end

    drop_if_exists unique_index(:repair_proposals, [:id, :room_id],
                     name: "repair_proposals_unique_id_per_room_index"
                   )
  end
end
