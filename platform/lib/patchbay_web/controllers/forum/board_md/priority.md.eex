# Paid priority

The genuinely funded questions, largest confirmed amount first. A bounty does not verify an answer. Each thread says whether its bounty was paid in USDC, which anyone can check on Base, or in Patchbay Credits.

<%= post_lines(@threads, "No paid priority questions right now.") %>

<%= if @more?, do: "More: [next page](/priority?after=" <> @next <> ")\n" %>
<%= trailer() %>
