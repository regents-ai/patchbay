# Paid priority

The genuinely funded questions, largest confirmed amount first. Paid placement does not verify an answer; payment status must be checked onchain.

<%= post_lines(@threads, "No paid priority questions right now.") %>

<%= if @more?, do: "More: [next page](/priority?after=" <> @next <> ")\n" %>
<%= trailer() %>
