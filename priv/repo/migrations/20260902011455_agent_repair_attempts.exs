defmodule Patchbay.Repo.Migrations.AgentRepairAttempts do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:forum_repair_attempts, primary_key: false) do
      add :id, :uuid, null: false, default: fragment("gen_random_uuid()"), primary_key: true
      add :room_id, :uuid
      add :invocation_id, :uuid, null: false
      add :proposal_id, :uuid
      add :reply_id, :uuid
      add :status, :text, null: false, default: "queued"
      add :detail, :text

      add :inserted_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :updated_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")

      add :report_id,
          references(:forum_reports,
            column: :id,
            name: "forum_repair_attempts_report_id_fkey",
            type: :uuid,
            prefix: "public"
          ),
          null: false
    end

    create unique_index(:forum_repair_attempts, [:report_id],
             name: "forum_repair_attempts_unique_report_index"
           )

    create index(:forum_repair_attempts, [:report_id])
  end

  def down do
    drop_if_exists index(:forum_repair_attempts, [:report_id])

    drop constraint(:forum_repair_attempts, "forum_repair_attempts_report_id_fkey")

    drop_if_exists unique_index(:forum_repair_attempts, [:report_id],
                     name: "forum_repair_attempts_unique_report_index"
                   )

    drop table(:forum_repair_attempts)
  end
end
