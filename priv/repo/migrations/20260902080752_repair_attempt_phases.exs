defmodule Patchbay.Repo.Migrations.RepairAttemptPhases do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:forum_repair_attempts) do
      add :phase, :text, null: false, default: "queued"

      add :phase_changed_at, :utc_datetime_usec,
        null: false,
        default: fragment("(now() AT TIME ZONE 'utc')")
    end
  end

  def down do
    alter table(:forum_repair_attempts) do
      remove :phase_changed_at
      remove :phase
    end
  end
end
